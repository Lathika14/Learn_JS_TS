String
* its collection of Characters
declare 2 types

Type 1:-
String Literal // store the value in String constant pool memory its part heap memory
let name="Di1ip" // 301
let // it will not assign to new memory

Type 2:-
String Object // directly it store value inside heap memory
let comName= new String("Test1eaf") // 202
let namel=new String( "TestLeaf") // 302

Declaring a String
Single quotes - >' '
Double quotes ->" "
Back Ticks - >` `